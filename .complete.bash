# complete -d cd        # Complete only directories after 'cd' command.
# complete -E -o dirnames # Complete directory names in completing an empty line.
complete -E -o default # Use 'readline' default completion.

